### 一、系统连接图

#### 1. 电源连接表（双实线）

| 起点 | 终点 | 说明 |
|------|------|------|
| 220V AC | L298N 电驱区 | 转换为 12V DC 输入 |
| L298N | STM32 | 5V 供电（L298N 内部集成降压输出） |
| STM32 3V3 | 1.3寸 OLED | 3.3V 逻辑供电 |
| L298N | 直流电机 | 驱动电压输出（单向） |

#### 2. 信号连接表（单实线）

| 信号源 | 信号目标 | 接口类型 | 方向 |
|--------|---------|---------|------|
| STM32 | L298N | 2×PWM | 单向（控制） |
| 正交编码器 | STM32 | 2路编码信号 | 单向（反馈） |
| STM32 | 1.3寸 OLED | I2C | 单向（显示） |
| 矩阵键盘 | STM32 | GPIO | **单向读取** |
| STM32 | DAPLINK | UART / SW | 双向（调试/下载） |

---

### 二、STM32F103C8T6 引脚分配表

#### 1. L298N 电机驱动控制
| STM32 引脚 | 定时器/功能 | L298N 引脚 |
|-----------|------------|-----------|
| PA7 | — | ENA |
| PB10 | TIM2 CH3 | IN1 |
| PB11 | TIM2 CH4 | IN2 |

#### 2. 内部定时任务
| 定时器 | 功能说明 |
|--------|---------|
| TIM3 | 内部定时器，用于完成定时任务（如控制周期中断） |

#### 3. OLED 显示屏（I2C1 + 3.3V 供电）
| STM32 引脚 | OLED 引脚 | 类型 |
|-----------|----------|------|
| 3.3V | VCC | 电源 |
| PB8 | SCL | 信号 |
| PB9 | SDA | 信号 |

#### 4. 矩阵键盘（4×4 矩阵，实际仅使用 4 个键，单向读取）
| STM32 引脚 | 键盘接口 | 工作模式 |
|-----------|---------|---------|
| 3.3V | R1 | 行线供电 |
| PA3 | C1 | **下拉输入 GPIO**（单向读取） |
| PA4 | C2 | **下拉输入 GPIO**（单向读取） |
| PA5 | C3 | **下拉输入 GPIO**（单向读取） |
| PA6 | C4 | **下拉输入 GPIO**（单向读取） |

> **注**：矩阵键盘为 4×4（16 键）规格，当前方案仅使用其中 **4 个按键**，STM32 通过下拉输入 GPIO **单向读取**键值。

#### 5. DAPLINK 调试接口（UART 双向）
| STM32 引脚 | 功能 | DAPLINK 引脚 | 通信方向 |
|-----------|------|-------------|---------|
| PB6 | TX | RX | MCU → DAPLINK |
| PB7 | RX | TX | DAPLINK → MCU |

#### 6. 电机编码器（正交编码输入捕获）
| STM32 引脚 | 编码器信号 |
|-----------|-----------|
| PA8 | A 相 |
| PA9 | B 相 |

### 三、电机参数

12V 201RPM 减速比1:21

空载转速201RPM 空载电流46mA

负载转速168RPM 电流300mA 扭矩0.53kg·cm 功率1.25W

堵转扭矩2.65kg·cm 电流1mA

减速器 减速比21

编码器参数:

AB双相增量式磁性霍尔编码器

基础脉冲11PPR*减速比

响应频率100kHz

集电极开漏输出

## 四、1.3寸OLED

使用硬件I2C+DMA传输控制OLED显示

OLED控制芯片 SH1106，参考: datasheets/Sinowealth-SH1106.pdf

ASCII字体参考: ref/font.h
